<Window x:Class="ProyectoListasdeReproduccion.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ProyectoListasdeReproduccion.ViewModels"
       Background="Black"
        xmlns:est="clr-namespace:ListasDeReproduccion.Views.EstadisticasView"
        xmlns:local="clr-namespace:ProyectoListasdeReproduccion.Views"
      xmlns:fa="http://schemas.fontawesome.com/icons/"
        mc:Ignorable="d"
        Title="MainView" Height="750" Width="1200">
    <Window.DataContext>
        <vm:MainViewModell></vm:MainViewModell>
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:CancionViewModel}">
            <local:CancionesIndexView></local:CancionesIndexView>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ListasViewModel}">
            <local:ListasIndexView></local:ListasIndexView>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:EstadisticaViewModel}">
            <est:EstadisticaView></est:EstadisticaView>
        </DataTemplate>
    </Window.Resources>
    <DockPanel>
        <StackPanel DockPanel.Dock="Left" Background="Black" Width="120">
            <Button Width="100" Height="100" Margin="10" Background="Gray" Command="{Binding NavegarListasCommand}">
                <fa:ImageAwesome Icon="Solid_ListUl"></fa:ImageAwesome>
            </Button>
            <Button  Width="100" Height="100" Margin="10" Background="Gray" Command="{Binding NavegarCancionesCommand}">
                <fa:ImageAwesome Icon="Solid_Music"></fa:ImageAwesome>
            </Button>
        </StackPanel>
        <Grid Background="Gray">

            <ContentControl  Background="LimeGreen" Content="{Binding VMactual}">
                
            </ContentControl>
        </Grid>
        
    </DockPanel>
</Window>
